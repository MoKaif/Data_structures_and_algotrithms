#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector <int> a1;
    for(int i=0;i<a1.end();i++)
    {
        ;
    }
    int max,min;
    max = *max_element(a.begin(),a.end());
    min = *min_element(a.begin(),a.end());
    cout<<max<<" "<<min;
    return 0;
}